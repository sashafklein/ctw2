= form_for [@user, @workout] do |f|
	
	= f.label :name, "Name (optional)"
	= f.text_field :name

	= f.label :description
	= f.text_area :description

	= f.label :score
	= f.text_field :score

	= f.submit "#{edit_or_log} your workout"